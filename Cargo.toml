[package]
name = "mtproxy"
version = "0.1.0"
edition = "2021"
authors = ["Seu Nome <email@exemplo.com>"]
description = "MTProxy - Proxy multifuncional (SOCKS5, WebSocket, modo direto)"
license = "MIT"

[dependencies]
tokio = { version = "1", features = ["io-util", "net", "macros", "rt-multi-thread", "sync", "time"] }
